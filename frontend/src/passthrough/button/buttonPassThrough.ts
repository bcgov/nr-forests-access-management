/**
 * PrimeVue v4 PassThrough Configuration for Button Component
 * Migrated from primevue-components-overrides.scss (lines 73-109)
 *
 * Handles layout/structure rules and focus state styling.
 * Colors are already managed by PrimeVue v4 preset in frontend/src/passthrough/primevue-preset.ts
 *
 * Uses FAM custom tokens (semantic design variables) for consistent theming
 */

export const BUTTON_PASS_THROUGH = {
  root: {
    class: 'fam-button',
  },
};
