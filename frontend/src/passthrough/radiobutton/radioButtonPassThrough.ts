/**
 * PrimeVue v4 PassThrough Configuration for RadioButton Component
 * Migrated from primevue-components-overrides.scss
 *
 * Uses FAM custom tokens (semantic design variables) for consistent theming.
 * Wired globally in main.ts via the `pt` option.
 */

export const RADIO_BUTTON_PASS_THROUGH = {
    root: {
        class: "fam-radiobutton",
    },
    box: {
        class: "fam-radiobutton-box",
    },
    icon: {
        class: "fam-radiobutton-icon",
    },
};