/**
 * PrimeVue v4 PassThrough Configuration for InputText Component
 * Migrated from primevue-components-overrides.scss
 *
 * Uses FAM custom tokens (semantic design variables) for consistent theming.
 * Wired globally in main.ts via the `pt` option.
 */

export const INPUT_PASS_THROUGH = {
    // Root input element
    root: {
        class: 'fam-inputtext',
    },
};
