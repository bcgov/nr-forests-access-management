export enum Severity {
    Success = 'success',
    Warning = 'warn',
    Error = 'error',
}

export enum ErrorCode {
    Conflict = 'Conflict',
    SelfGrantProhibited = 'SelfGrantProhibited',
    Default = 'Default',
}
