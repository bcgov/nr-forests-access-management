/**
 * Represents the different types of user codes used in the system.
 *
 * Possible values:
 * - `'B'`  : Business BCeID
 * - `'CD'` : User Type for DEV BCSC users
 * - `'CP'` : User Type for PROD BCSC users
 * - `'CT'` : User Type for TEST BCSC users
 * - `'I'`  : IDIR
 */
export type UserTypeCodeType = 'B' | 'CD' | 'CP' | 'CT' | 'I';

