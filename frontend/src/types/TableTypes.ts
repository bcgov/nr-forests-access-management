/**
 * Defines the column type used in PrimeVue Datatable.
 */
export type ColumnType = {
    field: string;
    header: string;
    sortable?: boolean;
    filterable?: boolean;
    width?: string;
};
